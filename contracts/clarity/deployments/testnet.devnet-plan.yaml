[network]
name = "testnet"
stacks_node_rpc_address = "https://api.testnet.hiro.so"
deployment_fee_rate = 10

[accounts.deployer]
mnemonic = "${STACKS_DEPLOYER_MNEMONIC}"

[plan.batches]

[[plan.batches]]
id = 0
transactions = [
    { contract-publish = { contract-name = "pulse", expected-sender = "$DEPLOYER", cost = 10000 } }
]
epoch = "3.0"
